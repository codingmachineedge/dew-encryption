#!/usr/bin/env bash
set -euo pipefail

if [ -n "${NAUTILUS_SCRIPT_SELECTED_FILE_PATHS:-}" ]; then
  first="$(printf '%s\n' "${NAUTILUS_SCRIPT_SELECTED_FILE_PATHS}" | head -n 1)"
else
  first="${PWD}"
fi

dew-encryption-gui "${first}" --history
